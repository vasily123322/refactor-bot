# 🤖 Руководство по AI-генерации контента

## ✅ Что реализовано

### 1. Сервис генерации (AIGenerationService)
- ✅ Подключение к OpenRouter API
- ✅ Генерация текста по теме с учётом пресетов
- ✅ Обработка переменных в промптах ({topic}, {tone}, {length}, {emoji_level}, {hashtags}, {cta})
- ✅ Модерация контента (запрещённые слова)
- ✅ Учёт лимитов токенов (дневной/месячный)
- ✅ Подсчёт использованных токенов

### 2. Интеграция в редактор постов
- ✅ Кнопка "🤖 ИИ" в редакторе
- ✅ Меню ИИ с опциями:
  - 📝 Генерация текста по теме
  - 🔗 Генерация из ссылки (в разработке)
  - ✨ Улучшить текст (в разработке)
- ✅ FSM состояния для ввода темы
- ✅ Автоматическая вставка сгенерированного текста в пост

### 3. Настройки каналов
- ✅ Полное UI для настройки ИИ в меню канала
- ✅ Выбор пресета из 7 готовых
- ✅ Настройка тона, длины, эмодзи, языка
- ✅ Настройка хештегов и CTA
- ✅ Модерация и запрещённые слова

## 🚀 Как использовать

### Шаг 1: Настройка OpenRouter API

1. Получите API ключ на https://openrouter.ai
2. Добавьте в `.env` или переменные окружения:
   ```env
   OPENROUTER_API_KEY=sk-or-v1-...
   OPENROUTER_MODEL=openai/gpt-4o-mini
   OPENROUTER_TEMPERATURE=0.7
   OPENROUTER_TOP_P=0.9
   ```

### Шаг 2: Включение ИИ для канала

1. Откройте **Настройки** → **Список каналов** → Выберите канал
2. Нажмите **🤖 ИИ**
3. Зайдите в любое подменю (Текст/Хештеги/Модерация)
4. ИИ автоматически включится при первой настройке

### Шаг 3: Настройка генерации

**ИИ → Текст:**
- Выберите пресет (Акция, Новость, Обучающий и т.д.)
- Или настройте свой промпт
- Установите тон: дружелюбный / экспертный / официальный / провокационный
- Длину: короткий / средний / длинный
- Эмодзи: 0-3

**ИИ → Хештеги/CTA:**
- Включите генерацию хештегов
- Выберите количество (1, 2, 3, 5, 7)
- Включите CTA (призыв к действию)

**ИИ → Модерация:**
- Включите модерацию
- Добавьте запрещённые слова (в разработке)
- Настройте разрешение ссылок и UTM

### Шаг 4: Генерация текста в редакторе

1. Создайте пост: **Создать пост** → Выберите канал
2. В редакторе нажмите кнопку **🤖 ИИ**
3. Выберите **📝 Генерация текста**
4. Введите тему, например:
   - "Акция на курсы Python со скидкой 40%"
   - "Обзор новых функций ChatGPT-4"
   - "Новость о запуске нового продукта"
5. Дождитесь генерации (5-30 секунд)
6. Сгенерированный текст автоматически вставится в пост
7. Отредактируйте при необходимости и опубликуйте

## 📊 Как работает генерация

### 1. Построение промпта

Сервис берёт пресет (или кастомный промпт) и подставляет переменные:

**Системный промпт (из пресета):**
```
Ты — копирайтер, специализирующийся на создании продающих постов для Telegram-каналов.
Твоя задача: написать короткий, цепляющий пост об акции или специальном предложении.
Стиль: {tone}
Язык: {lang}
```

**Пользовательский промпт:**
```
Тема: {topic}
Длина: {length}
Эмодзи: {emoji_level}
{hashtags}
{cta}
```

### 2. Вызов OpenRouter API

```python
{
  "model": "openai/gpt-4o-mini",
  "messages": [
    {"role": "system", "content": "Ты — копирайтер..."},
    {"role": "user", "content": "Тема: Акция на курсы..."}
  ],
  "temperature": 0.7,
  "top_p": 0.9,
  "max_tokens": 2000
}
```

### 3. Модерация

Если включена модерация, текст проверяется на:
- Запрещённые слова (замена на ***)
- Фильтры (в разработке)

### 4. Учёт токенов

После генерации:
- Инкрементируется `tokens_used_day` и `tokens_used_month`
- Проверяются лимиты перед следующей генерацией

## 🎯 Примеры промптов

### Акция (коротко)
**Системный промпт:**
```
Ты — копирайтер, специализирующийся на создании продающих постов для Telegram.
Стиль: {tone}
Язык: {lang}
```

**User:**
```
Тема: {topic}
Длина: короткий (2-3 абзаца)
Эмодзи: 1 (умеренно)
Добавь 3 хештега
Добавь призыв к действию в конце
```

### Новость (средний)
**Системный промпт:**
```
Ты — редактор новостного Telegram-канала.
Стиль: {tone}
Язык: {lang}
```

**User:**
```
Тема: {topic}
Длина: средний (3-5 абзацев)
Структура: заголовок, краткое изложение, детали, выводы
Эмодзи: 1
Добавь 3 хештега
```

## 🔧 Переменные промптов

Доступные переменные:
- `{topic}` — тема поста (обязательно)
- `{tone}` — тон (дружелюбный/экспертный/официальный/провокационный)
- `{length}` — длина (короткий/средний/длинный)
- `{emoji_level}` — уровень эмодзи (0-3)
- `{lang}` — язык (ru/en)
- `{hashtags}` — инструкция по хештегам
- `{cta}` — инструкция по CTA
- `{brand}` — название бренда (опционально)
- `{audience}` — аудитория (опционально)
- `{channel_title}` — название канала (опционально)

## 📈 Лимиты токенов

**Дневной лимит** — настраивается в `channel_ai_settings.tokens_limit_day`
**Месячный лимит** — настраивается в `channel_ai_settings.tokens_limit_month`

При превышении лимита генерация блокируется с сообщением:
- "Превышен дневной лимит токенов"
- "Превышен месячный лимит токенов"

**Сброс счётчиков:**
- Дневной — каждый день в 00:00 (реализуйте через cron/scheduler)
- Месячный — 1 числа каждого месяца

## 🐛 Обработка ошибок

### Ошибки API
- HTTP 401: Неверный API ключ
- HTTP 429: Превышен rate limit
- HTTP 500: Ошибка сервера OpenRouter

Все ошибки логируются в logger и возвращаются пользователю.

### Таймауты
Таймаут запроса к OpenRouter: 60 секунд

## 📝 TODO

- [ ] Генерация из ссылки (парсинг + саммари)
- [ ] Улучшение текста (рерайт, изменение тона)
- [ ] Генерация медиа через DALL-E/Midjourney
- [ ] UI для управления запрещёнными словами
- [ ] Автоматический сброс счётчиков токенов (scheduler)
- [ ] A/B тестирование вариантов
- [ ] Аналитика использования ИИ

## 🔗 Полезные ссылки

- OpenRouter API: https://openrouter.ai/docs
- Доступные модели: https://openrouter.ai/models
- Цены на токены: https://openrouter.ai/models (см. Pricing)

---

**Автор**: AI Assistant  
**Дата**: 2025-10-01  
**Версия**: 1.0.0